name: Deploy releases to multiple environments

on:
    workflow_dispatch: {}

jobs:
    # deploy:
    #     name: Deploy to test and prod environments
    #     permissions:
    #         id-token: write
    #         contents: read
    #     runs-on: ubuntu-latest
    #     steps:
    #         - run: echo "🎉 some text"

    ReuseableMatrixJobForDeployment:
        # needs: build
        strategy:
            fail-fast: true
            matrix:
                target: [test, prod]
        uses: ./.github/workflows/deploy.yml
        with:
            target-env: ${ { matrix.target } }
        # secrets: inherit
